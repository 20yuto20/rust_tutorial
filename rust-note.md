# Rustを勉強するにあたってのメモ的な場所
## chores
- 構造体の値を読み取って別の場所でそれを再利用する場合は基本的に '&self'で記述するのが適切

## [derive(...)] マクロについて
### `#[derive(...)]` を「自動で〇〇してくれる魔法の呪文」と考える

想像してみてください。あなたはゲームのキャラクターを作っています。

1.  **普通の作り方（マクロなし）**:
    キャラクターを作った後、「このキャラクターはデバッグ画面に表示できるようにする」「このキャラクターはコピーできるようにする」など、一つ一つ手作業で設定していく必要があります。これはちょっと面倒ですよね。

2.  **`#[derive(...)]` を使った作り方（マクロあり）**:
    キャラクターを作るときに、最初に「このキャラクターは**自動でデバッグ表示できるようにしてね**」「このキャラクターは**自動でコピーできるようにしてね**」という「魔法の呪文」を唱えます。

    この「魔法の呪文」が `#[derive(Debug, Clone)]` です。

    ```rust
    #[derive(Debug, Clone)] // ←これが「魔法の呪文」
    struct MyCharacter {
        name: String,
        hp: i32,
    }

    fn main() {
        let char1 = MyCharacter { name: "勇者".to_string(), hp: 100 };

        // 呪文のおかげで、デバッグ表示が自動でできる！
        println!("{:?}", char1); // → MyCharacter { name: "勇者", hp: 100 }

        // 呪文のおかげで、コピーも自動でできる！
        let char2 = char1.clone();
        println!("{:?}", char2); // → MyCharacter { name: "勇者", hp: 100 }
    }
    ```

    こうすると、あなたはキャラクターの基本的な情報（名前やHP）だけを定義すればよく、デバッグ表示やコピーといった「おまけの機能」は Rust が自動で用意してくれるわけです。

### `hoge` や `fuga` は「どんな魔法をかけるか」の指示

`#[derive(hoge, fuga)]` は、

  * `hoge`：例えば「デバッグ表示できるようにする魔法」
  * `fuga`：例えば「コピーできるようにする魔法」

というように、どんな種類の魔法をかけるか（どんな機能を自動で追加するか）を指示している部分です。

Rust では、この「魔法」のことを「**トレイト** (Trait)」と呼びます。

  * `Debug` トレイト：デバッグ表示できるようにする機能
  * `Clone` トレイト：コピーできるようにする機能
  * `PartialEq` トレイト：同じものか比較できるようにする機能

など、色々な「魔法」の種類があります。

### まとめると

`#[derive(hoge, fuga)]` は、

「この `struct` (構造体) や `enum` (列挙型) に、`hoge` という機能と `fuga` という機能を**自動で追加してね！**」

という、Rust コンパイラへのお願い（指示）です。

自分で面倒なコードを書かなくても、必要な機能をサッと追加できる、とても便利な仕組みなんです。

##　そもそもマクロとは

マクロとは何か？（シンプルに言うと）
マクロとは、一言でいうと「コードを生成するコード」です。

あなたが書いたプログラムが実際に動く前に、コンパイル（人間が書いたコードをコンピューターが理解できる形に変換する作業）という段階があります。マクロは、このコンパイルの途中で活躍します。

イメージとしては、あなたが書いたコードの一部を、マクロが「自動的に別のコードに書き換えてくれる」という感じです。

例え話で考えてみよう
あなたが料理人だと想像してください。

普通の料理（関数）:
あなたは「野菜を切る」「肉を焼く」「味付けをする」という具体的な手順（関数）を一つ一つ実行していきます。それぞれの材料（引数）に対して、決まった手順で処理を行います。

マクロを使った料理（マクロ）:
あなたはレシピの途中で「魔法の調味料セットを投入！」という指示を見つけました。この「魔法の調味料セット」がマクロです。

この魔法の調味料セットは、その場で「塩とコショウを小さじ1ずつ、ニンニクを一片、醤油を大さじ2」というように、具体的な調味料の組み合わせ（コード）を自動的に生成し、鍋に投入してくれます。

あなたは「魔法の調味料セットを投入！」と書くだけでよく、具体的な調味料の名前を全て手書きする必要がありません。しかも、このセットは料理の種類（コンパイル時の状況）によって、少しずつ調味料の組み合わせを変えることもできます。

つまり、マクロは「このレシピのこの部分に、こんな感じでコードを自動生成して差し込んでね！」という指示のようなものです。

マクロの主な特徴
コンパイル時に実行される: プログラムが実行される前（コンパイル時）に動きます。

コードを生成・変更する: あなたが書いたソースコードを、別の（より詳細な）ソースコードに変換します。

繰り返しを減らす（DRY: Don't Repeat Yourself）: 同じようなパターンで何度も書く必要があるコードを、マクロを使って一度定義するだけで済ませることができます。

メタプログラミング: プログラム自身が他のプログラム（この場合は自分のコード）を操作する、という高度なプログラミング手法の一種です。

なぜマクロが必要なのか？
マクロは、次のような課題を解決するために使われます。

ボイラープレートコードの削減:
「ボイラープレートコード」とは、毎回同じようなパターンで書かなければならない、退屈で繰り返し発生するコードのことです。
例: #[derive(Debug)] は、Debug トレイトを実装するための多くのボイラープレートコードを自動生成してくれます。

可変引数への対応:
普通の関数では引数の数が固定されていますが、マクロは任意の数の引数を受け取ることができます。
例: println! や format! は、引数の数が可変です。

ドメイン固有言語 (DSL) の作成:
特定の目的のために、より読みやすく直感的な構文を作ることができます。
例: vec! マクロは vec![1, 2, 3] のように、Pythonのリストのように簡単にベクタを作成できる構文を提供します。これは実際には std::vec::Vec::from([1, 2, 3]) のようなコードに展開されます。

コンパイル時チェックと最適化:
マクロはコンパイル時にコードを生成するため、生成されるコードに対してもコンパイラの厳密なチェックが及び、より安全で効率的なコードが生まれます。

Rustにおけるマクロの種類
Rust には主に2種類のマクロがあります。

宣言的マクロ (macro_rules!):
「こんな入力パターンを見つけたら、こんなコードに変換してね」というルールベースで定義されます。比較的手軽に書くことができます。先ほどの vec! マクロなどがこれに該当します。

手続き型マクロ (#[derive], #[attribute], proc_macro_name!):
より強力で、Rust のコードそのものを解析し、操作して新しいコードを生成します。自分でカスタマイズした #[derive] や、カスタム属性 (#[my_attribute]) などを作る際に使います。これは専用のクレート（ライブラリ）として実装する必要があります。

まとめ
マクロは「コードを生成するコード」。

コンパイル時に動いて、あなたの書いたコードの一部を自動的に別のコードに書き換える。

これにより、繰り返しのコードを減らしたり、柔軟な構文（可変引数など）を実現したりできる。

! が付いているものは、マクロである可能性が高い。

マクロはRustの強力な機能の一つですが、最初から全てを理解する必要はありません。まずは #[derive] や format!、println!、vec! といった既存のマクロが「裏側でコードを自動生成してくれているんだな」という感覚を掴むことが大切です。
